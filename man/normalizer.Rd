% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R, R/statistics_new.R
\name{normalizer}
\alias{normalizer}
\alias{vn_statistic}
\alias{vl_statistic}
\title{Self-normalizing statistic (paper: \eqn{V_n} and \eqn{\hat v_\ell})}
\usage{
normalizer(
  sample,
  k,
  m = c(m1 = 0, m2 = 0),
  support_set = NULL,
  n_points = 20,
  type = 1
)

vn_statistic(
  sample,
  k,
  m = c(m1 = 0, m2 = 0),
  support_set = NULL,
  n_points = 20
)

vl_statistic(
  sample,
  k,
  m = c(m1 = 0, m2 = 0),
  support_set = NULL,
  n_points = 20
)

normalizer(
  sample,
  k,
  m = c(m1 = 0, m2 = 0),
  support_set = NULL,
  n_points = 20,
  type = 1
)

vn_statistic(
  sample,
  k,
  m = c(m1 = 0, m2 = 0),
  support_set = NULL,
  n_points = 20
)

vl_statistic(
  sample,
  k,
  m = c(m1 = 0, m2 = 0),
  support_set = NULL,
  n_points = 20
)
}
\arguments{
\item{sample}{Numeric matrix (rows = n, columns = p).}

\item{k}{Integer index (1-based) denoting the change-point location.}

\item{m}{Trimming parameter(s). Either a nonnegative scalar (applied to
both sides) or a length-2 numeric giving \code{c(m1 = ..., m2 = ...)}.
Defaults to \code{c(m1 = 0, m2 = 0)}.}

\item{support_set}{Optional subset of coordinates (columns) to use, that is,
a set of integer indices. If \code{NULL}, all columns are used.}

\item{n_points}{Integer (paper: K). Number of points drawn from the
measure ν on (0, 1) used in the self-normalization construction.
Default is 20.}

\item{type}{Integer code selecting the normalization variant. Use \code{1}
for \eqn{V_n} and \code{0} for \eqn{\hat v_\ell}. In the estimation of the
set S, type 0 is used.}
}
\value{
Numeric scalar: the value of the self-normalization factor.

Numeric scalar: the value of the self-normalization factor.
}
\description{
Computes the self-normalization quantity used in the manuscript for the
statistic T_n. The argument \code{k} denotes the (candidate) change-point
location (1-based). The computation can be restricted to a subset of
coordinates (the paper's set S).

Computes the self-normalization quantity used in the manuscript for the
statistic T_n. The argument \code{k} denotes the (candidate) change-point
location (1-based). The computation can be restricted to a subset of
coordinates (the paper's set S).
}
\details{
An empty set results in a value of 0 for the normalizer.

An empty set results in a value of 0 for the normalizer.
}
\keyword{internal}

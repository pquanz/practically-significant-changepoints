% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{t_lambda_statistic}
\alias{t_lambda_statistic}
\alias{tn_statistic}
\title{T_n (k, m; λ) statistic (paper)}
\usage{
t_lambda_statistic(
  sample,
  k,
  m = c(m1 = 0, m2 = 0),
  lambda = 1,
  support_set = NULL
)

tn_statistic(sample, k, m = c(m1 = 0, m2 = 0), support_set = NULL)
}
\arguments{
\item{sample}{Numeric matrix or data frame with observations in rows (n)
and variables in columns (p).}

\item{k}{Integer index (1-based) denoting the change-point location.
Must lie within the valid range for the data size.}

\item{m}{Trimming parameter(s). Either a nonnegative scalar (applied to both
sides) or a length-2 numeric giving \code{c(m1 = ..., m2 = ...)}. Defaults to
\code{c(m1 = 0, m2 = 0)}.}

\item{lambda}{Tuning parameter (paper: λ). Numeric in (0, 1]; controls the
self-normalization subsample. Smaller values use a more restrictive subset;
\code{1} uses the full eligible subset. Default \code{1}.}

\item{support_set}{Optional subset of coordinates (columns) to use, that is,
a set of integer indices. If \code{NULL}, all columns are used.}
}
\value{
Numeric scalar: the value of T_n (k, m; λ).
}
\description{
Computes the statistic referred to in the manuscript as T_n (k, m; λ).
The argument \code{k} denotes the (candidate) change-point location in the data.
Although the statistic can be evaluated for any valid \code{k} with respect to
\code{n}, in applications \code{k} is typically interpreted as the change point.
}
\details{
An empty set \code{support_set} results in a value of 0 for the statistic.
}
\examples{
X <- matrix(rnorm(200 * 10), 200, 10)

# default trimming
t_lambda_statistic(X, k = 100)

# Scalar trimming for both sides
t_lambda_statistic(X, k = 120, m = 2)

# Scalar trimming with unequal trimming parameters
t_lambda_statistic(X, k = 120, m = c(m1 = 1, m2 = 3))
}

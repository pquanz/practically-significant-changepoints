% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics_new.R
\name{estimate_support_set}
\alias{estimate_support_set}
\title{Fast estimation of the support set (paper: \eqn{\hat S_n})}
\usage{
estimate_support_set(
  sample,
  k,
  m = c(m1 = 0, m2 = 0),
  kappa = 3/2,
  n_points = 20,
  parallelize = FALSE
)
}
\arguments{
\item{sample}{Numeric matrix (rows = n, columns = p).}

\item{k}{Integer index (1-based) denoting the change-point location.}

\item{m}{Trimming parameter(s). Either a nonnegative scalar or
a length-2 numeric giving \code{c(m1 = ..., m2 = ...)}.
Defaults to \code{c(m1 = 0, m2 = 0)}.}

\item{kappa}{Numeric exponent of \code{log(p)} in the set estimator.}

\item{n_points}{Integer (paper: K). Default is 20.}
}
\value{
Integer vector of indices, or \code{integer(0)}.
}
\description{
Drop-in replacement for \code{estimate_support_set} that vectorizes
across all p coordinates instead of looping.
Speedup is roughly p× in R overhead.
}
\examples{
X <- matrix(rnorm(200 * 10), 200, 10)
s_hat <- estimate_support_set(X, k = 100)
}

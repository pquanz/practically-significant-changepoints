% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{estimate_support_set}
\alias{estimate_support_set}
\alias{s_hat}
\title{Pivotal set estimator (paper: \eqn{\hat S_n})}
\usage{
estimate_support_set(
  sample,
  k,
  m = c(m1 = 0, m2 = 0),
  kappa = 3/2,
  n_points = 20,
  parallelize = FALSE
)

s_hat()
}
\arguments{
\item{sample}{Numeric matrix (rows = n, columns = p).}

\item{k}{Integer index (1-based) denoting the change-point location.}

\item{m}{Trimming parameter(s). Either a nonnegative scalar (applied to
both sides) or a length-2 numeric giving \code{c(m1 = ..., m2 = ...)}.
Defaults to \code{c(m1 = 0, m2 = 0)}.}

\item{kappa}{Numeric exponent of the logarithm \code{log(p)} in the definition
of the set estimator.}

\item{n_points}{Integer (paper: K). Number of points drawn from the
measure ν on (0, 1) used in the self-normalization construction.
Default is 20.}

\item{parallelize}{Logical; if \code{TRUE}, evaluates the internal loop in
parallel.}
}
\value{
List of indices or \code{integer(0)} if the empty set was estimated.
}
\description{
Computes the estimator for the support set S, which is a subset of 1:p.
}
\examples{
X <- matrix(rnorm(200 * 10), 200, 10)
s_hat <- estimate_support_set(X, k = 100)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trimming.R
\name{get_f1}
\alias{get_f1}
\alias{get_delta_f1}
\alias{get_f2}
\alias{get_delta_f2}
\alias{estimate_m}
\title{Estimation of trimming parameter \eqn{\hat m_n}}
\usage{
get_f1(sample, k, max_proportion_m = 1/3, parallelize = FALSE)

get_delta_f1(sample, k, max_proportion_m = 1/3, parallelize = FALSE)

get_f2(sample, k, max_proportion_m = 1/3, parallelize = FALSE)

get_delta_f2(sample, k, max_proportion_m = 1/3, parallelize = FALSE)

estimate_m(
  sample,
  k,
  threshold = 0.01,
  max_proportion_m = 1/3,
  method = "max",
  parallelize = FALSE
)
}
\arguments{
\item{sample}{Numeric matrix or data frame (rows = n, columns = p).
If a data frame is supplied, all columns must be numeric.}

\item{k}{Integer index (1-based) denoting the candidate change-point
location.}

\item{max_proportion_m}{Numeric in (0, 1]; caps the maximum \code{m} considered
as \code{floor(max_proportion_m * segment_length)}, where segment_length denotes
either the sample before or after the change point. Default \code{1/3}.}

\item{parallelize}{Logical; if \code{TRUE}, evaluates the internal loop in
parallel.}

\item{threshold}{Numeric scalar used by threshold-based rules (see \code{method}).
Default \code{0.01}.}

\item{method}{Character selecting the return value. Supported:
\itemize{
\item \code{"max"}: pick the maximum of m1 and m2 and return numeric m.
\item \code{"individual"}: keep both values m1 and m2 and return both as list.
Default \code{"max"}.
}}
}
\value{
A data frame with two columns:
\itemize{
\item \code{m} (integer): the trimming value/index evaluated (sorted ascending).
\item \code{f1} (numeric): the corresponding f1 value at that \code{m}.
}

A data frame with two columns:
\itemize{
\item \code{m} (integer): the trimming value/index evaluated (sorted ascending).
\item \code{f2} (numeric): the corresponding f2 value at that \code{m}.
}

Depending on method:
\itemize{
\item if \code{method} is set to \code{max}, it returns an integer scalar,
\item if \code{method} is set to \code{individual}, it returns a list
c(m1 = ..., m2 = ...)
}
}
\description{
Estimates the trimming parameter for a given change point
candidate \code{k} using the difference function Delta F1. The maximum
value can only be a proportion of the whole respective segmet
(see \code{max_proportion_m}).
}
\section{Functions}{
\itemize{
\item \code{get_f1()}: \eqn{F_1} function for the estimation of m (paper)

Computes the quantity denoted \eqn{F_1} in the manuscript for a given
candidate change-point location \code{k}. The input is a numeric matrix.
Trimming is capped by a proportion of the available segment length
(see \code{max_proportion_m}).

\item \code{get_delta_f1()}: Difference Function Delta F1

\item \code{get_f2()}: F_2 function for the estimation of m (paper)

Computes the quantity denoted F_2 in the manuscript for a given
candidate change-point location \code{k}. The input is a numeric matrix.
Trimming is capped by a proportion of the available segment length
(see \code{max_proportion_m}).

\item \code{get_delta_f2()}: Difference Function Delta F2

}}
\section{Parallelization}{

If \code{parallelize = TRUE}, this function uses the \strong{future.apply} backend.
It runs \strong{sequentially} unless you set a future plan with more than one
worker. In your script/session, do for example:

\preformatted{
  # choose a plan (Windows/macOS/Linux)
  future::plan(future::multisession, workers = 4)

  # run your code here ...

  # return to sequential mode
  future::plan(future::sequential)
}

Reproducibility: set a seed \strong{before} calling (e.g., \code{set.seed(1)});
internally we use \code{future.seed = TRUE}. Package code and dependencies are
shipped to workers automatically; just ensure any packages used internally
are listed in \verb{Imports:}/\verb{Suggests:} in \code{DESCRIPTION}.
}

\examples{
X <- matrix(rnorm(200 * 10), 200, 10)
get_f1(X, k = 100)
get_f1(X, k = 120, max_proportion_m = 1 / 4, parallelize = TRUE)
get_delta_f1(X, k = 100)
X <- matrix(rnorm(200 * 10), 200, 10)
get_f2(X, k = 100)
get_f2(X, k = 120, max_proportion_m = 1 / 4, parallelize = TRUE)
get_delta_f2(X, k = 100)
X <- matrix(rnorm(200 * 10), 200, 10)
estimate_m(X, k = 120)
estimate_m(X, k = 120, method = "individual", threshold = 0.02)
}
\seealso{
\link{get_f1}, \code{\link[=get_delta_f1]{get_delta_f1()}}, \code{\link[=get_f2]{get_f2()}}, \code{\link[=get_delta_f2]{get_delta_f2()}}
}
